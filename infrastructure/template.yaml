AWSTemplateFormatVersion: "2010-09-09"
Description: AFL Odds Scraper — Sportsbet H2H odds to S3 on a schedule

Parameters:
  Environment:
    Type: String
    AllowedValues: [dev, prod]
    Description: Deployment environment

  ArtifactBucket:
    Type: String
    Description: S3 bucket containing the Lambda deployment zip

  ArtifactKey:
    Type: String
    Default: afl-odds/lambda.zip
    Description: S3 key of the Lambda zip package

  ScheduleEnabled:
    Type: String
    AllowedValues: [ENABLED, DISABLED]
    Default: ENABLED
    Description: Whether the EventBridge schedule is active

  LambdaTimeout:
    Type: Number
    Default: 120
    Description: Lambda timeout in seconds

  LambdaMemory:
    Type: Number
    Default: 128
    Description: Lambda memory in MB

Conditions:
  IsProd: !Equals [!Ref Environment, prod]

Resources:
  # ── S3 ──────────────────────────────────────────────────────────────────
  ResultsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub afl-odds-${Environment}-${AWS::AccountId}
      LifecycleConfiguration:
        Rules:
          - Id: ExpireOldOdds
            Status: Enabled
            ExpirationInDays: !If [IsProd, 365, 30]
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Project
          Value: afl-odds-scraper

  # ── IAM ─────────────────────────────────────────────────────────────────
  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub afl-odds-lambda-${Environment}
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: S3ResultsWrite
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - s3:PutObject
                Resource: !Sub arn:aws:s3:::afl-odds-${Environment}-${AWS::AccountId}/odds/*

  # ── Lambda ──────────────────────────────────────────────────────────────
  ScraperFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub afl-odds-scraper-${Environment}
      Description: Fetches AFL H2H odds from Sportsbet and writes JSONL to S3
      Runtime: python3.12
      Handler: handler.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Code:
        S3Bucket: !Ref ArtifactBucket
        S3Key: !Ref ArtifactKey
      Timeout: !Ref LambdaTimeout
      MemorySize: !Ref LambdaMemory
      Environment:
        Variables:
          RESULTS_BUCKET: !Ref ResultsBucket
          ENVIRONMENT: !Ref Environment
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Project
          Value: afl-odds-scraper

  # ── EventBridge Scheduler ────────────────────────────────────────────────
  SchedulerRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub afl-odds-scheduler-${Environment}
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: scheduler.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: InvokeScraper
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: lambda:InvokeFunction
                Resource: !GetAtt ScraperFunction.Arn

  # Every 4 hours, 9am–9pm Melbourne time — DST-aware via Australia/Melbourne timezone
  ScraperSchedule:
    Type: AWS::Scheduler::Schedule
    Properties:
      Name: !Sub afl-odds-schedule-${Environment}
      Description: Trigger AFL odds scraper every 4 hrs between 9am and 9pm Melbourne time
      ScheduleExpression: "cron(0 9,13,17,21 * * ? *)"
      ScheduleExpressionTimezone: Australia/Melbourne
      State: !Ref ScheduleEnabled
      FlexibleTimeWindow:
        Mode: "OFF"
      Target:
        Arn: !GetAtt ScraperFunction.Arn
        RoleArn: !GetAtt SchedulerRole.Arn

Outputs:
  ResultsBucketName:
    Description: S3 bucket where odds files are written
    Value: !Ref ResultsBucket
    Export:
      Name: !Sub afl-odds-results-bucket-${Environment}

  LambdaFunctionName:
    Description: Lambda function name
    Value: !Ref ScraperFunction
    Export:
      Name: !Sub afl-odds-function-${Environment}

  LatestOddsS3Path:
    Description: S3 path for latest odds snapshot
    Value: !Sub s3://afl-odds-${Environment}-${AWS::AccountId}/odds/latest.jsonl
